apiVersion: apps/v1
kind: Deployment
metadata:
  name: schedule-service
  namespace: winvestco
  labels:
    app: schedule-service
    component: service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: schedule-service
  template:
    metadata:
      labels:
        app: schedule-service
        component: service
    spec:
      containers:
        - name: schedule-service
          image: ${DOCKERHUB_NAMESPACE}/schedule-service:latest
          ports:
            - containerPort: 8095
              name: http
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: OTEL_SERVICE_NAME
              value: "schedule-service"
            - name: POSTGRES_DB_NAME
              value: "schedule_service"
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8095
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8095
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: schedule-service
  namespace: winvestco
  labels:
    app: schedule-service
spec:
  selector:
    app: schedule-service
  ports:
    - port: 8095
      targetPort: 8095
      name: http
  type: ClusterIP
