apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: winvestco
  labels:
    app: notification-service
    component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        component: service
    spec:
      containers:
        - name: notification-service
          image: ${DOCKERHUB_NAMESPACE}/notification-service:latest
          ports:
            - containerPort: 8089
              name: http
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          env:
            - name: OTEL_SERVICE_NAME
              value: "notification-service"
            - name: POSTGRES_DB_NAME
              value: "notification_service"
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8089
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8089
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: winvestco
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - port: 8089
      targetPort: 8089
      name: http
  type: ClusterIP
