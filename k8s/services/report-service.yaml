apiVersion: apps/v1
kind: Deployment
metadata:
  name: report-service
  namespace: winvestco
  labels:
    app: report-service
    component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: report-service
  template:
    metadata:
      labels:
        app: report-service
        component: service
    spec:
      containers:
        - name: report-service
          image: ${DOCKERHUB_NAMESPACE}/report-service:latest
          ports:
            - containerPort: 8091
              name: http
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: OTEL_SERVICE_NAME
              value: "report-service"
            - name: POSTGRES_DB_NAME
              value: "report_service"
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8091
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8091
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: winvestco
  labels:
    app: report-service
spec:
  selector:
    app: report-service
  ports:
    - port: 8091
      targetPort: 8091
      name: http
  type: ClusterIP
