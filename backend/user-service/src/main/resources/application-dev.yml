# Development Profile - Local development
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/winvestco_user_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

  redis:
    host: localhost
    port: 6379

  rabbitmq:
    host: localhost

  flyway:
    repair-on-migrate: true
    validate-on-migrate: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

jwt:
  secret: dev-secret-key-for-local-development-only-change-in-production-minimum-256-bits
  expiration: 86400000

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    in.winvestco.user_service: DEBUG

server:
  port: 8088
