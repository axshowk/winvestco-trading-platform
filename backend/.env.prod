# ================================
# WINVESTCO Production Environment
# ================================
# ⚠️  NEVER commit this file with real values!
# ⚠️  Use Kubernetes Secrets, AWS SSM, HashiCorp Vault, etc.
# ⚠️  All values are MANDATORY in production

# ================================
# Spring Profile
# ================================
SPRING_PROFILES_ACTIVE=prod

# ================================
# Database Configuration (REQUIRED)
# ================================
SPRING_DATASOURCE_URL=<PROD_DATABASE_URL>
SPRING_DATASOURCE_USERNAME=<PROD_DATABASE_USERNAME>
SPRING_DATASOURCE_PASSWORD=<PROD_DATABASE_PASSWORD>

# ================================
# Eureka Configuration (REQUIRED)
# ================================
# Use comma-separated URLs for HA cluster
EUREKA_SERVER_URL=<PROD_EUREKA_CLUSTER_URLS>

# ================================
# JWT Configuration (REQUIRED)
# ================================
# CRITICAL: Generate strong secret: openssl rand -base64 64
JWT_SECRET=<PROD_JWT_SECRET_MIN_256_BITS>
JWT_EXPIRATION=86400000

# ================================
# Google OAuth2 (REQUIRED)
# ================================
GOOGLE_CLIENT_ID=<PROD_GOOGLE_CLIENT_ID>
GOOGLE_CLIENT_SECRET=<PROD_GOOGLE_CLIENT_SECRET>

# ================================
# Message Broker Configuration (REQUIRED)
# ================================
RABBITMQ_HOST=<PROD_RABBITMQ_HOST>
RABBITMQ_PASSWORD=<PROD_RABBITMQ_PASSWORD>

# ================================
# Redis Configuration (REQUIRED)
# ================================
SPRING_REDIS_HOST=<PROD_REDIS_HOST>
SPRING_REDIS_PORT=6379
SPRING_REDIS_PASSWORD=<PROD_REDIS_PASSWORD>

# ================================
# Kafka Configuration (REQUIRED)
# ================================
SPRING_KAFKA_BOOTSTRAP_SERVERS=<PROD_KAFKA_BROKERS>

# ================================
# Razorpay (LIVE Mode - CRITICAL)
# ================================
# ⚠️  These are LIVE payment credentials!
RAZORPAY_KEY_ID=<PROD_RAZORPAY_LIVE_KEY_ID>
RAZORPAY_KEY_SECRET=<PROD_RAZORPAY_LIVE_KEY_SECRET>

# ================================
# Report Service
# ================================
REPORT_STORAGE_PATH=<PROD_REPORT_STORAGE_PATH>

# ================================
# Observability (REQUIRED)
# ================================
OTEL_EXPORTER_OTLP_ENDPOINT=<PROD_JAEGER_ENDPOINT>
OTEL_TRACES_SAMPLER_ARG=0.1

# ================================
# Trade Service
# ================================
# Set to false for production to disable mock execution
MOCK_EXECUTION_ENABLED=false
